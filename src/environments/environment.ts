// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyBpk8uuoJOioStFQ4NMF_LGauM63yM65u4",
    authDomain: "maxim-blog.firebaseapp.com",
    databaseURL: "https://maxim-blog.firebaseio.com",
    projectId: "maxim-blog",
    storageBucket: "maxim-blog.appspot.com",
    messagingSenderId: "409760280540",
    appId: "1:409760280540:web:2b1420a4b03d91748d6919"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
